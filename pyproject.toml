[project]
name = "ai-conversation-progress"
version = "0.1.0"
description = "A simple, elegant application for tracking progress on tasks and subtasks with visual feedback."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyqt5>=5.15.11",
]
